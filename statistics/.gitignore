# Compiled source #
###################
*.com
*.class
*.dll
*.exe
*.o
*.so

# Packages #
############
# it's better to unpack these files and commit the raw source
# git has its own built in compression methods
*.7z
*.dmg
*.gz
*.iso
*.jar
*.rar
*.tar
*.zip
*.war
*.ear

# Logs and databases #
######################
*.log
*.sql
*.sqlite

# OS generated files #
######################
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

#Eclipse
.metadata/
.classpath
.project
.settings/
.svn/
target/
Servers/
/target

#these files get modified a lot for each test run, so ignore them
#specifying the files explicitly so the .gitignore will still be there and new checkouts will have this folder
src/main/resources/gitCommits/commits.txt
src/main/resources/gitCommits/gitlog.txt